{% extends 'base.html.twig' %}

{% block title %}Accueil - PhpSquad{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            background: linear-gradient(135deg, #0a0e27 0%, #1a1a3e 50%, #0f1b3c 100%);
            background-attachment: fixed;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                radial-gradient(circle at 20% 50%, rgba(0, 255, 100, 0.1) 0%, transparent 50%),
                linear-gradient(rgba(0, 255, 100, 0.02) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 255, 100, 0.02) 1px, transparent 1px);
            background-size: 100% 100%, 50px 50px, 50px 50px;
            pointer-events: none;
            z-index: 0;
        }
        .example-wrapper { 
            margin: 3em auto; 
            max-width: 800px; 
            width: 95%; 
            font: 18px/1.5 'Courier New', monospace;
            background: rgba(0, 20, 0, 0.95);
            padding: 2rem;
            border: 3px solid #0f0;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 255, 0, 0.5);
            position: relative;
            z-index: 1;
            color: #0f0;
        }
        .example-wrapper h1 {
            color: #0f0;
            text-shadow: 0 0 10px rgba(0, 255, 0, 0.8);
            border-bottom: 2px solid #0f0;
            padding-bottom: 0.5rem;
            margin-bottom: 1rem;
        }
        .example-wrapper code { 
            background: rgba(0, 0, 0, 0.6); 
            padding: 4px 8px;
            border: 1px solid #0f0;
            border-radius: 3px;
            color: #0ff;
            font-family: 'Courier New', monospace;
        }
        .example-wrapper ul {
            list-style-type: none;
            padding-left: 0;
        }
        .example-wrapper li {
            margin: 1rem 0;
            padding-left: 1.5rem;
            position: relative;
        }
        .example-wrapper li::before {
            content: '▶';
            position: absolute;
            left: 0;
            color: #0f0;
        }
    </style>
{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>/home/wassim/Documents/Etudes/BTS_SIO2/Projet/phpSquad/src/Controller/AccueilController.php</code></li>
        <li>Your template at <code>/home/wassim/Documents/Etudes/BTS_SIO2/Projet/phpSquad/templates/accueil/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
